基于JVM的语言，JetBrains开发
可以编译成Java字节码，也可以编译成JavaScript

特点
	空类型安全：变量默认不为空，为空编译会报错。当然可以设置允许为空。Java等很多语言需要做大量的避免空指针的判断
	lambda表达式更简洁
	扩展函数：可以对现有的类添加新的函数，不用修改原来的类。调用时该函数会以导入的方式添加到这个类中
	类型推导：var，高版本java也开始支持var
	既可做Android开发，也可做服务端开发。可以与Java互操作
	不用写分号
	支持协程