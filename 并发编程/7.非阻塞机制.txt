实现复杂，可伸缩性和活跃性好。
趋势：非阻塞、乐观锁、细粒度的锁

CAS 需要操作系统指令的支持
	
原子变量类
	与volatile有相同的语义
	
	
非阻塞算法
	非阻塞的栈、链表...
	
ABA问题
	在CAS中将判断V的值是否为A并依此进行下一步的操作，但是V的值从A到B又变回A，这个时候V是否被认为发生了变化？
	解决：如使用版本号
	
双重检查加锁DCL
	避免这种方式 如实现单例之一的那个双重检查加锁的方式
	因为线程可能看到一个仅被部分构造的对象