分区键
	根据某个区间值(范围值)、特定值列表或hash值执行数据的聚集，让数据根据规则分布在不同的分区中，让一个大对象变成一些小对象。
	
myisam innodb都支持分区表	
分区是对一个表的所有数据和索引进行分区
如果有主键或唯一键，则分区键必须是主键或者唯一键

分区类型
	RANGE分区：基于一个给定连续区间范围，把数据分配到不同的分区
	LIST分区：基于枚举出的值列表进行分区
	HASH分区：基于给定的分区个数，把数据hash取模的值作为分区键
	KEY分区：类似hash分区
	
子分区
	对分区表中每个分区再次分割